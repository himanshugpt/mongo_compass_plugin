const Reflux = require('reflux');

const CompassPluginActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'exportButtonClicked',
  'importButtonClicked'
]);

module.exports = CompassPluginActions;
